ws-beans
--------
These are the build environments for various schema generated beans for use with CTREP. They should only have to be run once, to populate a maven2 repository. After that, they are referenced via maven dependencies in the other modules.